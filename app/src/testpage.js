import React, { 
    useEffect, 
    useState 
} from 'react';

const Testpage = () => {
    const [data, setData] = useState(null);

    return (
        <p>succ of ses</p>
    );
}

export default Testpage;